# ════════════════════════════════════════════════════════════════════
#  ORIGINAL training environment — reproduces the published models
#  Platform: NVIDIA A100-SXM4 (CUDA 12.8)
#
#  INSTALL (two steps — torch stack MUST come from the cu128 index):
#    pip install torch==2.10.0 torchvision==0.25.0 \
#        --index-url https://download.pytorch.org/whl/cu128
#    pip install -r requirements_original.txt
#
#  NOTE: This pins the environment that TRAINED the original models. The
#  original *caption metrics* were computed with a non-official scorer
#  (missing PTBTokenizer) and are understated; re-score with the official
#  pipeline (pycocoevalcap + PTBTokenizer) regardless of this environment.
# ════════════════════════════════════════════════════════════════════

# ── Core numerics ───────────────────────────────────────────────────────────
numpy==1.26.4

# ── torch stack (installed separately from cu128 — see header) ──────────────
#   torch==2.10.0
#   torchvision==0.25.0

# ── Training / inference stack (original published versions) ────────────────
transformers==5.0.0
peft==0.18.1
bitsandbytes==0.49.2
accelerate==1.13.0
huggingface_hub==1.18.0
datasets==4.0.0
tensorboard==2.20.0
scipy==1.16.2
pillow==11.3.0
tqdm==4.67.3
requests==2.32.4

# ── Scoring (predictions → official metrics) ────────────────────────────────
# score_vizwiz_vqa.py uses only the standard library (no package needed).
# score_captions.py uses pycocotools + pycocoevalcap (PTBTokenizer needs Java:
#   apt-get install -y default-jre).
# NOTE: verify pycocoevalcap==1.2 reproduces your validated CIDEr (~1.24); if you
# originally used the salaniz fork, replace with:
#   pycocoevalcap @ git+https://github.com/salaniz/pycocoevalcap.git
pycocotools==2.0.10
pycocoevalcap==1.2
