# Requirements for Jetson Orin Nano Deployment
# JetPack 6.0+ (Ubuntu 22.04, CUDA 12.6)
#
# Installation Instructions:
# 1. Run: pip3 install -r requirements-jetson.txt
# 2. Follow setup steps in deployment/jetson_setup.sh
#
# Note: PyTorch must be installed separately from Jetson AI Lab repository

# Core ML Libraries (install after PyTorch)
numpy==1.26.4
huggingface_hub>=0.23.0
transformers>=4.44.0
accelerate>=0.34.0
peft>=0.13.0
scipy>=1.11.0

# Image Processing
pillow>=10.0.0
opencv-python-headless==4.9.0.80

# Quantization (see BUILD_BITSANDBYTES.md for compilation)
# bitsandbytes>=0.43.0  # Must be compiled from source on Jetson

# Audio & Speech
SpeechRecognition==3.10.1
pyttsx3==2.90
PyAudio==0.2.14

# Utilities
requests>=2.31.0
pyyaml>=6.0.1
tqdm>=4.66.0
einops>=0.7.0

# Data handling
datasets>=2.18.0
pandas>=2.2.0

# Configuration
omegaconf>=2.3.0

# Optional: For development/debugging
# jupyter>=1.0.0
# matplotlib>=3.8.0
# ipywidgets>=8.1.0
